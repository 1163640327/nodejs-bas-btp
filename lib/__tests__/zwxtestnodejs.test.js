const assert = require('assert');
const zwxtestnodejs = require('../index.js');

describe('zwxtestnodejs', () => {
  it('has a test', () => {
    assert(false, 'zwxtestnodejs should have a test');
  });
});
